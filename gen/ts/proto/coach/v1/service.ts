// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               unknown
// source: proto/coach/v1/service.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export const protobufPackage = "jobmate.coach.v1";

export interface AnalyzeJobRequest {
  userId: string;
  jobId: string;
  /** On passe le texte brut ou l'ID si le coach a accès à la DB */
  jobDescriptionText: string;
}

export interface AnalysisResult {
  /** 0 à 100 */
  matchScore: number;
  /** Points forts */
  pros: string[];
  /** Points faibles */
  cons: string[];
  /** Mots clés ATS manquants */
  missingKeywords: string[];
}

export interface GenerateCoverLetterRequest {
  userId: string;
  jobDescription: string;
  /** "Formal", "Casual", "Enthusiastic" */
  tone: string;
}

export interface GenerateCoverLetterResponse {
  content: string;
}

function createBaseAnalyzeJobRequest(): AnalyzeJobRequest {
  return { userId: "", jobId: "", jobDescriptionText: "" };
}

export const AnalyzeJobRequest: MessageFns<AnalyzeJobRequest> = {
  encode(message: AnalyzeJobRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.jobId !== "") {
      writer.uint32(18).string(message.jobId);
    }
    if (message.jobDescriptionText !== "") {
      writer.uint32(26).string(message.jobDescriptionText);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AnalyzeJobRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAnalyzeJobRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.jobId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.jobDescriptionText = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AnalyzeJobRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      jobId: isSet(object.jobId)
        ? globalThis.String(object.jobId)
        : isSet(object.job_id)
        ? globalThis.String(object.job_id)
        : "",
      jobDescriptionText: isSet(object.jobDescriptionText)
        ? globalThis.String(object.jobDescriptionText)
        : isSet(object.job_description_text)
        ? globalThis.String(object.job_description_text)
        : "",
    };
  },

  toJSON(message: AnalyzeJobRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.jobId !== "") {
      obj.jobId = message.jobId;
    }
    if (message.jobDescriptionText !== "") {
      obj.jobDescriptionText = message.jobDescriptionText;
    }
    return obj;
  },

  create(base?: DeepPartial<AnalyzeJobRequest>): AnalyzeJobRequest {
    return AnalyzeJobRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AnalyzeJobRequest>): AnalyzeJobRequest {
    const message = createBaseAnalyzeJobRequest();
    message.userId = object.userId ?? "";
    message.jobId = object.jobId ?? "";
    message.jobDescriptionText = object.jobDescriptionText ?? "";
    return message;
  },
};

function createBaseAnalysisResult(): AnalysisResult {
  return { matchScore: 0, pros: [], cons: [], missingKeywords: [] };
}

export const AnalysisResult: MessageFns<AnalysisResult> = {
  encode(message: AnalysisResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.matchScore !== 0) {
      writer.uint32(8).int32(message.matchScore);
    }
    for (const v of message.pros) {
      writer.uint32(18).string(v!);
    }
    for (const v of message.cons) {
      writer.uint32(26).string(v!);
    }
    for (const v of message.missingKeywords) {
      writer.uint32(34).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AnalysisResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAnalysisResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.matchScore = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pros.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.cons.push(reader.string());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.missingKeywords.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AnalysisResult {
    return {
      matchScore: isSet(object.matchScore)
        ? globalThis.Number(object.matchScore)
        : isSet(object.match_score)
        ? globalThis.Number(object.match_score)
        : 0,
      pros: globalThis.Array.isArray(object?.pros) ? object.pros.map((e: any) => globalThis.String(e)) : [],
      cons: globalThis.Array.isArray(object?.cons) ? object.cons.map((e: any) => globalThis.String(e)) : [],
      missingKeywords: globalThis.Array.isArray(object?.missingKeywords)
        ? object.missingKeywords.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.missing_keywords)
        ? object.missing_keywords.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: AnalysisResult): unknown {
    const obj: any = {};
    if (message.matchScore !== 0) {
      obj.matchScore = Math.round(message.matchScore);
    }
    if (message.pros?.length) {
      obj.pros = message.pros;
    }
    if (message.cons?.length) {
      obj.cons = message.cons;
    }
    if (message.missingKeywords?.length) {
      obj.missingKeywords = message.missingKeywords;
    }
    return obj;
  },

  create(base?: DeepPartial<AnalysisResult>): AnalysisResult {
    return AnalysisResult.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AnalysisResult>): AnalysisResult {
    const message = createBaseAnalysisResult();
    message.matchScore = object.matchScore ?? 0;
    message.pros = object.pros?.map((e) => e) || [];
    message.cons = object.cons?.map((e) => e) || [];
    message.missingKeywords = object.missingKeywords?.map((e) => e) || [];
    return message;
  },
};

function createBaseGenerateCoverLetterRequest(): GenerateCoverLetterRequest {
  return { userId: "", jobDescription: "", tone: "" };
}

export const GenerateCoverLetterRequest: MessageFns<GenerateCoverLetterRequest> = {
  encode(message: GenerateCoverLetterRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.jobDescription !== "") {
      writer.uint32(18).string(message.jobDescription);
    }
    if (message.tone !== "") {
      writer.uint32(26).string(message.tone);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenerateCoverLetterRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenerateCoverLetterRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.jobDescription = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.tone = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenerateCoverLetterRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      jobDescription: isSet(object.jobDescription)
        ? globalThis.String(object.jobDescription)
        : isSet(object.job_description)
        ? globalThis.String(object.job_description)
        : "",
      tone: isSet(object.tone) ? globalThis.String(object.tone) : "",
    };
  },

  toJSON(message: GenerateCoverLetterRequest): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.jobDescription !== "") {
      obj.jobDescription = message.jobDescription;
    }
    if (message.tone !== "") {
      obj.tone = message.tone;
    }
    return obj;
  },

  create(base?: DeepPartial<GenerateCoverLetterRequest>): GenerateCoverLetterRequest {
    return GenerateCoverLetterRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GenerateCoverLetterRequest>): GenerateCoverLetterRequest {
    const message = createBaseGenerateCoverLetterRequest();
    message.userId = object.userId ?? "";
    message.jobDescription = object.jobDescription ?? "";
    message.tone = object.tone ?? "";
    return message;
  },
};

function createBaseGenerateCoverLetterResponse(): GenerateCoverLetterResponse {
  return { content: "" };
}

export const GenerateCoverLetterResponse: MessageFns<GenerateCoverLetterResponse> = {
  encode(message: GenerateCoverLetterResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.content !== "") {
      writer.uint32(10).string(message.content);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenerateCoverLetterResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenerateCoverLetterResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.content = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenerateCoverLetterResponse {
    return { content: isSet(object.content) ? globalThis.String(object.content) : "" };
  },

  toJSON(message: GenerateCoverLetterResponse): unknown {
    const obj: any = {};
    if (message.content !== "") {
      obj.content = message.content;
    }
    return obj;
  },

  create(base?: DeepPartial<GenerateCoverLetterResponse>): GenerateCoverLetterResponse {
    return GenerateCoverLetterResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GenerateCoverLetterResponse>): GenerateCoverLetterResponse {
    const message = createBaseGenerateCoverLetterResponse();
    message.content = object.content ?? "";
    return message;
  },
};

export type CoachServiceService = typeof CoachServiceService;
export const CoachServiceService = {
  /** Analyse une offre spécifique par rapport à un profil */
  analyzeJob: {
    path: "/jobmate.coach.v1.CoachService/AnalyzeJob",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: AnalyzeJobRequest): Buffer => Buffer.from(AnalyzeJobRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): AnalyzeJobRequest => AnalyzeJobRequest.decode(value),
    responseSerialize: (value: AnalysisResult): Buffer => Buffer.from(AnalysisResult.encode(value).finish()),
    responseDeserialize: (value: Buffer): AnalysisResult => AnalysisResult.decode(value),
  },
  /** Génère une lettre de motivation */
  generateCoverLetter: {
    path: "/jobmate.coach.v1.CoachService/GenerateCoverLetter",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: GenerateCoverLetterRequest): Buffer =>
      Buffer.from(GenerateCoverLetterRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GenerateCoverLetterRequest => GenerateCoverLetterRequest.decode(value),
    responseSerialize: (value: GenerateCoverLetterResponse): Buffer =>
      Buffer.from(GenerateCoverLetterResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GenerateCoverLetterResponse => GenerateCoverLetterResponse.decode(value),
  },
} as const;

export interface CoachServiceServer extends UntypedServiceImplementation {
  /** Analyse une offre spécifique par rapport à un profil */
  analyzeJob: handleUnaryCall<AnalyzeJobRequest, AnalysisResult>;
  /** Génère une lettre de motivation */
  generateCoverLetter: handleUnaryCall<GenerateCoverLetterRequest, GenerateCoverLetterResponse>;
}

export interface CoachServiceClient extends Client {
  /** Analyse une offre spécifique par rapport à un profil */
  analyzeJob(
    request: AnalyzeJobRequest,
    callback: (error: ServiceError | null, response: AnalysisResult) => void,
  ): ClientUnaryCall;
  analyzeJob(
    request: AnalyzeJobRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: AnalysisResult) => void,
  ): ClientUnaryCall;
  analyzeJob(
    request: AnalyzeJobRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: AnalysisResult) => void,
  ): ClientUnaryCall;
  /** Génère une lettre de motivation */
  generateCoverLetter(
    request: GenerateCoverLetterRequest,
    callback: (error: ServiceError | null, response: GenerateCoverLetterResponse) => void,
  ): ClientUnaryCall;
  generateCoverLetter(
    request: GenerateCoverLetterRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GenerateCoverLetterResponse) => void,
  ): ClientUnaryCall;
  generateCoverLetter(
    request: GenerateCoverLetterRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GenerateCoverLetterResponse) => void,
  ): ClientUnaryCall;
}

export const CoachServiceClient = makeGenericClientConstructor(
  CoachServiceService,
  "jobmate.coach.v1.CoachService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): CoachServiceClient;
  service: typeof CoachServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
